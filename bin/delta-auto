#!/bin/bash
# Delta wrapper that auto-detects system theme

if [[ $(defaults read -g AppleInterfaceStyle 2>/dev/null) == "Dark" ]]; then
  exec delta --syntax-theme='Dracula' "$@"
else
  exec delta --syntax-theme='GitHub' --light "$@"
fi
