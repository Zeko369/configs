# Repo-wide Valkey overrides.
#
# Homebrew's stock Valkey config is included first by the generated
# `$(brew --prefix)/etc/valkey.conf` wrapper. These settings intentionally stay
# small so upgrades continue to inherit upstream defaults.

databases 128
